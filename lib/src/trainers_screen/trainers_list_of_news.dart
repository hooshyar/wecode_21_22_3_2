import 'package:flutter/material.dart';

class TrainersListOfNewsScreen extends StatelessWidget {
  const TrainersListOfNewsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('implement news'),
    );
  }
}
